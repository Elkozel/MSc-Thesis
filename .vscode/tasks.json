{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate and Upload SIF File",
            "type": "shell",
            "command": "cd ${workspaceFolder}; bash ${workspaceFolder}/scripts/generate_sif.sh",
        },
        {
            "label": "Run SIF on DAIC",
            "type": "shell",
            "command": "cd ${workspaceFolder}; ${workspaceFolder}/scripts/run_daic.sh"
        }
    ]
}